BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[KPI] ALTER COLUMN [time_limit] FLOAT(53) NOT NULL;

-- AlterTable
ALTER TABLE [dbo].[Reminder] ALTER COLUMN [time_limit] FLOAT(53) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
